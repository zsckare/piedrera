<%= form_for(@rutum) do |f| %>
  <% if @rutum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rutum.errors.count, "error") %> prohibited this rutum from being saved:</h2>

      <ul>
      <% @rutum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  
    <div class="input-field s12 m12 l10 offset-l1">
      <%= f.label :nombre %>
      <%= f.text_field :nombre %>
    </div>
</div>

<div class="row">
  <div class="actions">
    <%= f.submit %>
  </div>
</div>
<% end %>
