<div class="row">
  <div class="col s12 m12 l12">
    <p id="notice"><%= notice %></p>

  <div class="row">
    <h4 class="center-align">Lista de Camiones</h4>
  </div>

  <div class="row">
    <table>
  <thead>
    <tr>
      <th>Numero</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trucks.each do |truck| %>
      <tr>
        <td><%= truck.numero %></td>
        <td class="right" >
        <%= link_to 'Ver', truck, class: 'btn blue' %>
        <%= link_to 'Editar', edit_truck_path(truck), class: 'btn' %>
        <%= link_to 'Eliminar', truck, method: :delete, data: { confirm: '¿Desea Eliminar Este Registro?' }, class: 'btn red' %></td>
      </tr>
    <% end %>
  </tbody>
</table>
  </div>

<div class="row">
  <%= link_to 'Nuevo Camion', new_truck_path, class: 'btn blue' %>  
</div>


  </div>
</div>